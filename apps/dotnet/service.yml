apiVersion: v1
kind: Service
metadata:
  name: aspnet-core-service
  labels:
    app: aspnet-core
spec:
  type: LoadBalancer
  selector:
    app: aspnet-core
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80